---
type Props = {
  title: string;
  emoji: string;
  createdAt: string;
  updatedAt?: string;
  tags: string[];
};

import { MdDetail } from "@/components/MdDetail";

const { title, createdAt,updatedAt, emoji, tags } = Astro.props;
---

<MdDetail title={title} emoji={emoji} createdAt={createdAt} updatedAt={updatedAt} tags={tags}>
  <slot />
</MdDetail>
